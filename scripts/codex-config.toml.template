# Codex config: base_url points to codex-proxy (Responses API ↔ Chat Completions).
model = "__MODEL_NAME__"
model_provider = "local"

# Model metadata (auto-updated by start scripts to match the actual server config)
model_context_window = __CTX_WINDOW__
model_reasoning_effort = "low"

[model_providers.local]
name = "llama.cpp"
base_url = "http://127.0.0.1:__PROXY_PORT__/v1"
wire_api = "responses"

[profiles.local]
model = "__MODEL_NAME__"
model_provider = "local"
web_search = "disabled"
