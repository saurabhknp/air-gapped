# Codex config for local llama.cpp server (air-gapped). Bootstrap sets __PORT__ and __MODEL_NAME__.
model = "__MODEL_NAME__"
model_provider = "proxy"

[model_providers.proxy]
name = "llama.cpp"
base_url = "http://127.0.0.1:__PORT__/v1"
wire_api = "responses"
